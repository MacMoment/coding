import type { Metadata } from 'next';
import './globals.css';
import { ThemeProvider } from '@/components/theme-provider';
import { Toaster } from '@/components/ui/toaster';

export const metadata: Metadata = {
  title: 'ForgeCraft AI - Build Minecraft Plugins & Discord Bots with AI',
  description:
    'AI-powered builder for Minecraft plugins and Discord bots. Generate, edit, and deploy your projects with ease.',
  keywords: ['Minecraft', 'Discord', 'AI', 'Plugin', 'Bot', 'Generator'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
